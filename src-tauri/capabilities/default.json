{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Phase 0 shell. Permissions are added per feature, never broadened preemptively (docs/SECURITY.md §2).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "updater:default"
  ]
}
